# Fonts

## missing unicode characters (especially eastern symbols)

Noto is split into multiple fonts. The `noto-fonts` is only for generic symbols, not for Chinese, Japanese, or Korean. `noto-fonts-cjk` is needed in addition. This is basically required, as symbols in these fonts are often used as decoration.

Furthermore `noto-fonts-emoji` should be installed as well.
